# دليل اختبار المراحل المنفصلة
# Testing Individual Phases Guide

## ملفات الاختبار المتاحة

تم إنشاء 5 ملفات لتجربة كل مرحلة بشكل منفصل:

### 1️⃣ اختبار المحلل اللغوي (Lexer)
```bash
python3 test_lexer.py
```
**الوظيفة**: يعرض كيف يتم تحويل الكود العربي إلى رموز (Tokens)

**الناتج**: قائمة بجميع الرموز المستخرجة مع إحصائيات

---

### 2️⃣ اختبار المحلل النحوي (Parser)
```bash
python3 test_parser.py
```
**الوظيفة**: يعرض كيف يتم بناء شجرة البرنامج (AST) من الرموز

**الناتج**: عرض مرئي لبنية البرنامج على شكل شجرة

---

### 3️⃣ اختبار المحلل الدلالي (Semantic Analyzer)
```bash
python3 test_semantic.py
```
**الوظيفة**: يختبر اكتشاف الأخطاء المنطقية (متغيرات غير معرفة، دوال مكررة، إلخ)

**الناتج**: نتائج 5 اختبارات مختلفة (بعضها يجب أن ينجح والبعض يجب أن يفشل)

---

### 4️⃣ اختبار مولد الكود (Code Generator)
```bash
python3 test_codegen.py
```
**الوظيفة**: يعرض كود Assembly المُولّد من البرنامج العربي

**الناتج**: 
- كود Assembly كامل مع أرقام الأسطر
- إحصائيات عن الكود المولد
- ملف `test_output.s` يحتوي على الكود

---

### 5️⃣ التجربة الكاملة (Full Demo)
```bash
python3 demo.py
```
**الوظيفة**: يشغل جميع المراحل معًا من البداية للنهاية

**الناتج**:
- عرض تفصيلي لكل مرحلة
- تجميع وربط البرنامج تلقائيًا
- تشغيل البرنامج وعرض الناتج النهائي

---

## الترتيب المقترح للتجربة

ننصح بتجربة الملفات بالترتيب التالي:

```bash
# 1. ابدأ بالمحلل اللغوي
python3 test_lexer.py

# 2. ثم المحلل النحوي
python3 test_parser.py

# 3. ثم المحلل الدلالي
python3 test_semantic.py

# 4. ثم مولد الكود
python3 test_codegen.py

# 5. أخيرًا التجربة الكاملة
python3 demo.py
```

---

## تعديل الأمثلة

كل ملف يحتوي على متغير `arabic_code` في أعلى الملف. يمكنك تعديله لتجربة كود عربي مختلف:

```python
arabic_code = """
دالة رئيسية() {
    // ضع كودك هنا
    متغير س = 100؛
    اطبع(س)؛
    ارجع 0؛
}
"""
```

---

## أمثلة سريعة

### مثال 1: اختبار المحلل اللغوي
```bash
cd /home/mostafa/Work/arabic_compiler
python3 test_lexer.py
```

**الناتج المتوقع**: قائمة برموز مثل:
```
Token(FUNCTION, 'دالة')
Token(IDENTIFIER, 'رئيسية')
Token(LPAREN, '(')
...
```

### مثال 2: التجربة الكاملة
```bash
cd /home/mostafa/Work/arabic_compiler
python3 demo.py
```

**الناتج المتوقع**: 
- عرض جميع المراحل السبع
- تشغيل البرنامج تلقائيًا
- عرض الناتج النهائي (55 في المثال الافتراضي - مجموع 1+2+...+10)

---

## نصائح

1. **لرؤية المزيد من التفاصيل**: افتح كل ملف في محرر نصوص لفهم الكود

2. **لحفظ النتائج**: أعد توجيه الناتج لملف
   ```bash
   python3 test_lexer.py > lexer_output.txt
   ```

3. **للتجريب السريع**: عدّل المتغير `arabic_code` في أي ملف

4. **لفهم الأخطاء**: جرب `test_semantic.py` - يعرض أمثلة للأخطاء الشائعة

---

## المساعدة

إذا واجهت مشكلة في أي ملف:

1. تأكد أنك في المجلد الصحيح:
   ```bash
   cd /home/mostafa/Work/arabic_compiler
   ```

2. تأكد من وجود جميع الملفات الأساسية:
   ```bash
   ls *.py
   ```

3. تأكد من صلاحيات التشغيل:
   ```bash
   chmod +x test_*.py demo.py
   ```

---

**استمتع بالتجربة! 🚀**
